{
    "scripts": {
        "validate:json": "eslint typedoc/**/*.json && eslint sassdoc/**/*.json"
    },
    "devDependencies": {
        "eslint": "^7.12.1",
        "eslint-plugin-json": "^2.1.2"
    }
}
